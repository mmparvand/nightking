# Infrastructure

This directory can store IaC artifacts (Terraform, Ansible) and operational scripts for the VPN panel. The Docker Compose stack at the repo root is the primary local orchestration entrypoint.
